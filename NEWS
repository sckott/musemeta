musemeta 0.0.4.99
===============

BUG FIXES

* Fixed non-ascii characters. Added parameter `ascii`, with default `FALSE`, so that non-ascii
characters by default are removed. But you can set to `TRUE` to keep them.


musemeta 0.0.3.99
===============

BUG FIXES

* Fixed problem with `met()` - wasn't parsing content correctly, somehow using `httr >= 0.6.1` seems to work (#9)
* Changed all `scrapi_*()` functions to return message that the API is temporarily down (#8)
